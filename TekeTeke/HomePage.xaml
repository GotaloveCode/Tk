<Page
    x:Class="TekeTeke.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TekeTeke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Background="#225689" Orientation="Horizontal">
                <AppBarButton Icon="AllApps"/>
                <TextBlock Text="Scan QR" x:Uid="ScanQr" FontSize="{StaticResource TextStyleExtraLargeFontSize}" VerticalAlignment="Center"/>
            </StackPanel>
            <Grid Grid.Row="1" >
                <Grid.Background>
                    <ImageBrush Stretch="Fill" ImageSource="Assets/bg2.jpg"/>
                </Grid.Background>
                <Image Grid.Row="1" x:Name="QRCode" Width="250" Height="250" Margin="20,0,0,0" HorizontalAlignment="Left"/>
            </Grid>


        </Grid>
    </ScrollViewer>
</Page>
